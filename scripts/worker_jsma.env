# Worker 3 of 3 — JSMA-greedy, B=1 (lowest VRAM, different algorithm)
# Run as: WORKER_ENV=worker_jsma.env bash scripts/run_worker.sh
export WORKER_STRATEGY="jsma"
export WORKER_PORT="9203"
export WORKER_HOST="0.0.0.0"
export WORKER_DEADLINE_S="9.5"
# MPS — share GPU with other workers without serializing.
export CUDA_MPS_PIPE_DIRECTORY=/tmp/nvidia-mps
export CUDA_MPS_LOG_DIRECTORY=/tmp/nvidia-log
